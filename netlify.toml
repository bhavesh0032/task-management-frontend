[
build
]
  command = "npm run build"
  publish = "build"

[
build.environment
]
  NODE_VERSION = "20" # or whichever version you're using

[[
redirects
]]
  from = "/*"
  to = "/index.html"
  status = 200